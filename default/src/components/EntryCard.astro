---
import type { Entry, InteractionCounts } from '@indiepub/types';
import NoteCard from './NoteCard.astro';
import ArticleCard from './ArticleCard.astro';
import PhotoCard from './PhotoCard.astro';
import BookmarkCard from './BookmarkCard.astro';

interface Props {
  entry: Entry;
  counts?: InteractionCounts | undefined;
}

const { entry, counts } = Astro.props;
---

<article
  class:list={['entry-card', `entry-card--${entry.type}`]}
  itemscope
  itemtype="http://schema.org/BlogPosting"
>
  {entry.type === 'article' ? (
    <ArticleCard entry={entry} counts={counts} />
  ) : entry.type === 'photo' && (entry.attachments?.length ?? 0) > 0 ? (
    <PhotoCard entry={entry} counts={counts} />
  ) : entry.type === 'bookmark' && entry.bookmarkOf ? (
    <BookmarkCard entry={entry} counts={counts} />
  ) : (
    <NoteCard entry={entry} counts={counts} />
  )}
</article>

<style>
  .entry-card {
    background-color: var(--color-surface-1);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }
</style>
